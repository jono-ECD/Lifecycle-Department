# Blessed Botanicals

| | |
|---|---|
| **Slug** | `blessed-botanicals` |
| **Klaviyo account** | `RaFbmF` (verified) |
| **Data access** | Klaviyo MCP + Hiro (id 128074) |
| **Registry entry** | [`clients/registry.yml`](../registry.yml) |

Account identity lives in the registry, not here. This file is the human-facing
context: what is true about this account right now.

## Account context
_Business model, product catalog, seasonality, sending cadence, known constraints._

<!-- TODO: fill on next account review -->

## Active workstreams
_Which `system/` services are running for this client, and where they stand._

| Workstream | `system/` reference | Status | Owner |
|---|---|---|---|
| — | — | — | — |

## Working notes
_Decisions, gotchas, and anything the next person (or agent) needs to not relearn._

---
Create subfolders here (`strategy/`, `implementation/`, `reporting/`) when there is
real work to put in them — not before. Follow the method in `system/`; store the
output here.
