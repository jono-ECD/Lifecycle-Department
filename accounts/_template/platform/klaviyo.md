# Klaviyo — <Account name>

The table below is **generated from `accounts/registry.yml`** once this account has
a registry entry. Add the entry, then run `python3 scripts/sync.py` — it will
insert the generated block here.

Add these markers where the table should go:

```
<!-- BEGIN:generated:platform-klaviyo -->
placeholder
<!-- END:generated:platform-klaviyo -->
```

No secrets in this file. API keys live in the team's secret manager; this records
the public account identifier and which connectors are wired.

## Naming conventions
_Flow, segment, list, and tag naming used in this account._

## Integration notes
_Catalog feed, custom metrics, known tracking gaps._
